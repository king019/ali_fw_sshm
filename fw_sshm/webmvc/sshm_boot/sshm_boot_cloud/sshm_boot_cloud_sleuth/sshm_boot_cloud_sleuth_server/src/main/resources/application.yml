server:
  port: 8806
spring:
  application:
    name: sleuth_server
  zipkin:
    baseUrl: http://127.0.0.1:8807
  sleuth :
    enabled : true
eureka:
  client:
    serviceUrl:
      defaultZone: http://${register.center.host}:${register.center.port}/eureka