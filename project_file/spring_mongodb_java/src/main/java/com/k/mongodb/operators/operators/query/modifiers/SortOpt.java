package com.k.mongodb.operators.operators.query.modifiers;

import com.k.mongodb.operators.util.junit.query.JCQueryOpt;

import org.junit.Test;
import org.junit.Ignore;

/**
 * <br/> $natural(page 480) A special sort order that orders documents using the order of documents on disk.
 */
public class SortOpt extends JCQueryOpt {

  @Ignore
  @Test
  public void natural() {
    nouse();
  }
}
