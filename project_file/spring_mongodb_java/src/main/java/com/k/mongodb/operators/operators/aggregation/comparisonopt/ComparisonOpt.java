package com.k.mongodb.operators.operators.aggregation.comparisonopt;
import com.k.mongodb.operators.util.junit.aggregation.JCAggregationOpt;
/**
 * <br/>
 * $cmp(page 460) Compares two values and returns the result of the comparison as an integ <br/>
 * $eq(page 460) Takes two values and returns true if the values are equivalent. <br/>
 * $gt(page 460) Takes two values and returns true if the first is larger than the second. <br/>
 * $gte(page 460) Takes two values and returns true if the first is larger than or equal to the <br/>
 * $lt(page 460) Takes two values and returns true if the second value is larger than the fir <br/>
 * $lte(page 460) Takes two values and returns true if the second value is larger than or equ <br/>
 * $ne(page 461) Takes two values and returns true if the values arenotequivalent.
 */
public class ComparisonOpt extends JCAggregationOpt {
}
