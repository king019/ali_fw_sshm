package com.k.mongodb.operators.operators.query.projection;
import com.k.mongodb.operators.util.junit.query.JCQueryOpt;
/**
 * <br/>
 * $(page 403) Projects the first element in an array that matches the query condition. <br/>
 * $elemMatch (page 405) Projects only the first element from an array that matches the specified$elemMatch (page 405) condition. <br/>
 * $meta(page 407) Projects the document’s score assigned during$text(page 384) operation. <br/>
 * $slice(page 408) Limits the number of elements projected from an array. Supports skip and limit slices.
 */
public class Projection extends JCQueryOpt {
}
