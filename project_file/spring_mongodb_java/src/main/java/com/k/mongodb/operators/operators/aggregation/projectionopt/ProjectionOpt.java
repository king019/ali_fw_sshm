package com.k.mongodb.operators.operators.aggregation.projectionopt;
import com.k.mongodb.operators.util.junit.aggregation.JCAggregationOpt;
/**
 * <br/>
 * <br/>
 * $map(page 468) Applies a sub-expression to each item in an array and returns the res sub-expression. <br/>
 * $let(page 468) Defines variables for use within the scope of an aggregation expressi <br/>
 * $literal(page 469) Forces the aggregation pipeline to return a literal value without evalu
 */
public class ProjectionOpt extends JCAggregationOpt {
}
